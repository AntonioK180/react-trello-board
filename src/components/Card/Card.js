import React from 'react';
import PropTypes from 'prop-types';
import './Card.css';

const Card = (props) => (
  <div className="Card">
    Card Component
  </div>
);

Card.propTypes = {};

Card.defaultProps = {};

export default Card;
