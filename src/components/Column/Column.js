import React from 'react';
import PropTypes from 'prop-types';
import './Column.css';

const Column = (props) => (
  <div className="Column">
    Column Component
  </div>
);

Column.propTypes = {};

Column.defaultProps = {};

export default Column;
